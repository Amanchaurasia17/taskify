# TASKIFY Task Management - Environment Variables

# Frontend Environment (Optional - defaults are provided)
VITE_API_URL=http://localhost:5000/api

# Development Notes:
# - The frontend will automatically use http://localhost:5000/api if this is not set
# - Make sure both frontend (port 5173) and backend (port 5000) are running
# - The backend .env file should be in the server/ directory
